import React, { FC } from "react";

type EnquriesProps = {
  className?: string;
};

const Enquries: FC<EnquriesProps> = (props): JSX.Element => {
  return <div className="Enquries">Enquries</div>;
};

export default Enquries;
